# راهنمای مشارکت

> با مشارکت در این پروژه می‌پذیرید که از
> [منشور رفتاری](CODE_OF_CONDUCT.md) پیروی کنید.

خوش آمدید. هر اندازه کمک ارزشمند است، از اصلاح یک غلط املایی تا نوشتن یک
راهنمای یا اخبار.

## سریع‌ترین راه

پایین هر صفحهٔ سایت دکمهٔ **«ویرایش این صفحه در گیت‌هاب»** هست. اشتباهی دیدید،
همان‌جا اصلاحش کنید.

## راه‌اندازی محلی
پیشنهاد می‌کنم که ابتدا پروژه را [fork](https://github.com/goudarz/tuxly/fork) کنید بعد این مراحل را بروید.

برای هر تغییر در یک [Branch](https://git-scm.com/book/fa/v2/%d8%a7%d9%86%d8%b4%d8%b9%d8%a7%d8%a8%e2%80%8c%da%af%db%8c%d8%b1%db%8c-%d8%af%d8%b1-%da%af%db%8c%d8%aa-Git-Branching-%d8%b4%d8%a7%d8%ae%d9%87%e2%80%8c%d8%a8%d9%86%d8%af%db%8c-%d9%88-%d8%a7%d8%af%d8%ba%d8%a7%d9%85-%d9%be%d8%a7%db%8c%d9%87%e2%80%8c%d8%a7%db%8c-Basic-Branching-and-Merging) مجزا انجام بدید.

و همچنین به جای کلمه `USERNAME` نام‌کاربری گیت‌هاب خودتان را وارد کنید
```bash
git clone https://github.com/USERNAME/tuxly.git
cd tuxly
nvm use            # Node 24
npm install
npm run build      # الزامی است، ایندکس جست‌وجو را می‌سازد
npm run dev
```

اگر `npm run build` را رد کنید، همه‌چیز کار می‌کند جز جست‌وجو، چون ایندکس
Pagefind هنوز وجود ندارد.

## چه چیزی کمک می‌کند

- **اصلاح**: غلط املایی، لینک شکسته، اطلاعات کهنه
- **مطلب جدید**: خبر، معرفی، آموزش، بررسی
- **معرفی موجودیت**: توزیع، میزکار، پنجره‌گردان، پروژه
- **[اجتماع و گروه](#معرفی-یک-اجتماع-یا-گروه)**: با راه‌های ارتباطشان
- **[رویداد و ارائه‌دهنده](#افزودن-رویداد-و-ارائهدهنده)**: نشست، کارگاه، کنفرانس
- **[سالگرد](#افزودن-سالگرد)**: تاریخ‌های مربوط به نرم‌افزار آزاد
- **[پروفایل نویسنده](#پروفایل-نویسنده)**: اگر می‌خواهید بنویسید
- **[پوستر و تصویر](#ساخت-پوستر-و-تصویر)**: با اسکریپت، بدون ابزار گرافیکی
- **واژه‌نامه**: معادل فارسی اصطلاح فنی در `content/glossary/terms.json` (فعلا در همین حد پیش می‌ریم واسه این پروژه بعد این قسمت قرار است مجزا و گسترده‌تر بهش بپردازیم)
- **ترجمه**: کمک به فارسی‌سازی خود پروژه‌های متن‌باز
- **دوسویه و مشکلات مربوط به زبان فارسی**: کمک به پروژه‌های متن‌باز برای رفع مشکلات دوسویه و مواردی که ارتباطی به زبان فارسی دارند(زبان‌های دیگه‌ای مثل عربی، اردو و... هم که این موارد شامل حالشان می‌شود مدلی قاعدتا پیش خواهد رفت که همه اجتماع‌ها از این مدل تلاش سودی مستقیم یا غیرمستقیم بهشان برسد و اگر اون‌ها خواستن در آینده مواردی را رفع کننده برای زبان فارسی هم اعمال بشود)

## نوشتن

از `content/posts/_template.md` کپی بگیرید. چند نکته:

**زمینهٔ فارسی اجباری است.** اگر مطلب از منبع بیرونی می‌آید، فیلد `context`
باید بگوید این موضوع برای خوانندهٔ فارسی‌زبان چه معنایی دارد. بدون آن بیلد
می‌شکند و درست هم همین است، چون بدون زمینه مطلب ارزش افزوده‌ای ندارد.

**پیش‌نویس‌های خودکار مستقیم منتشر نمی‌شوند.** خط لولهٔ `pipeline/` پیش‌نویس
می‌سازد با `draft: true` و `originStatus: draft-review`. تا فردی بازبینی
نکند و `reviewedBy` را پر نکند، منتشر نمی‌شود.

**نسخه‌های توزیع‌ها را دستی ننویسید.** فیلدهای `currentVersion`، `releasedAt` و `eolAt`
را اسکریپت خودکار پر می‌کند. اگر دستی بنویسید، اجرای بعدی رونویسی‌شان می‌کند.

## نگارش فارسی

- نیم‌فاصله سر جایش: «می‌شود» نه «می شود»، «نرم‌افزار» نه «نرم افزار»
- «ی» و «ک» فارسی، نه عربی
- نام لاتین نرم‌افزارها را ترجمه نکنید: «GNOME»، نه «گنوم» در متن فنی،
  ولی در عنوان و متن روان فارسی‌نویسی اشکالی ندارد
- برای اصطلاح فنی، اول `content/glossary/terms.json` را ببینید تا معادل‌ها
  در کل سایت یکدست بماند
- عدد را فارسی ننویسید(الزامی نیست)؛ قالب‌بندی خودکار انجام می‌شود

## پیام کامیت

قالب Conventional Commits، **به انگلیسی**. جزئیات و نمونه‌ها در
[`docs/COMMITS.md`](docs/COMMITS.md).

```
content(distro): add Void Linux entry
fix(rtl): keep version numbers in order inside Persian sentences
```

## قبل از ارسال PR

```bash
npm run check     # باید صفر خطا بدهد
npm run build     # باید بدون خطا تمام شود
```

## تصویر

همهٔ تصویرها در `src/assets/` می‌روند، دسته‌بندی‌شده بر اساس نوع محتوا:

```
src/assets/
  events/      پوستر رویدادها
  posts/       تصویر مطالب
  entities/    نشان توزیع‌ها و پروژه‌ها
  authors/     آواتار نویسندگان
```

در frontmatter با **مسیر نسبی** به آن اشاره کنید. Astro در فایل محتوا فقط
مسیر نسبی را می‌فهمد و `~/` یا `@/` کار نمی‌کند:

```yaml
# از content/events/ → دو پله بالا
cover: ../../src/assets/events/tabriz-lug.jpg

# از content/posts/
cover: ../../src/assets/posts/my-post.jpg

# از content/entities/communities/ → سه پله
logo: ../../../src/assets/entities/tabriz-lug.svg
```

چند قاعده:

- **همیشه** `alt` بنویسید، بدون آن بیلد می‌شکند
- تبدیل به AVIF و WebP و ساخت `srcset` خودکار است؛ فایل اصلی را بهینه
  نکنید و با بالاترین کیفیتی که دارید بگذارید
- نام فایل را با اسلاگ همان محتوا یکی کنید تا بعداً پیدا کردنش آسان باشد
- آواتار نویسنده: مربعی ۵۱۲×۵۱۲، `webp` یا `png`، زیر ۲۰۰ کیلوبایت
- پوستر رویداد: هر نسبتی، ولی عرض دست‌کم ۱۲۰۰ پیکسل

> تصویرها را در `public/` نگذارید. آنجا فایل‌ها همان‌طور که هستند کپی
> می‌شوند و هیچ بهینه‌سازی‌ای نمی‌گیرند. `public/` فقط برای چیزهایی است که
> باید نشانی ثابت داشته باشند: نشان‌های برند، قلم‌ها، `robots.txt`.

## معرفی یک اجتماع یا گروه

فایل جدید در `content/entities/communities/` بسازید. از
`_template.md` کپی بگیرید.

مهم‌ترین بخشش `links` است، دکمه‌های راه ارتباط که کنار وب‌سایت و مخزن
می‌نشینند، بیست نوع دارد و هرکدام آیکون و برچسب خودش را می‌گیرد:

```yaml
links:
  - kind: telegram-channel
    url: https://t.me/example
  - kind: telegram-group
    url: https://t.me/example_group
    label: گروه گفت‌وگو        # برچسب پیش‌فرض را عوض می‌کند
    note: توضیح کوتاه هنگام هاور
```

انواع موجود: `telegram-channel`، `telegram-group`، `mastodon`، `matrix`،
`discord`، `slack`، `zulip`، `irc`، `forum`، `mailing-list`،
`stackoverflow`، `youtube`، `peertube`، `x`، `linkedin`، `bluesky`،
`wiki`، `blog`، `calendar`، `other`.

با `priority` می‌شود گروهی را بالای فهرست ثابت کرد - عدد کمتر، بالاتر.
پیش‌فرض ۱۰۰ است و ترتیب بقیه الفبایی می‌ماند(این عدد را تغییر ندهید).

## افزودن رویداد و ارائه‌دهنده

از `content/events/_template.md` کپی بگیرید.

**ارائه‌دهندگان صفحهٔ خودشان را خودکار می‌گیرند.** لازم نیست فایلی بسازید؛
نامی که در `performers` بنویسید کافی است و `/speakers/<نام>` خودش ساخته
می‌شود:

```yaml
performers:
  - نام ارائه‌دهنده

  - name: نام ارائه‌دهنده     # وقتی رویداد چند ارائه دارد
    talk: عنوان ارائه
    role: توسعه‌دهندهٔ وب      # اختیاری
    url: https://example.org  # اختیاری
    author: goudarz-jafari    # اگر نویسندهٔ سایت هم هست
```

اگر `author` بدهید، نام به پروفایل نویسنده لینک می‌شود و صفحهٔ جداگانه
ساخته نمی‌شود، تا یک نفر دو هویت نگیرد.

هویت ارائه‌دهنده از اسلاگ نامش می‌آید و فارسی اول نرمال می‌شود، پس
« سینا بی‌مثل» و «سینا بی مثل» چون یک نفرند، یک صفحه واسشون ساخته میشه در آينده اگر دو ارائه‌دهنده  با نام و فامیلی یکسان وجود داشتن همان موقع این قسمت توسعه داده میشه که مشکلی پیش نیایید.

## پروفایل نویسنده

فایل جدید در `content/authors/` بسازید، از `_template.md` کپی کنید.

آواتار: مربعی ۵۱۲×۵۱۲، `webp` یا `png`، زیر ۲۰۰ کیلوبایت، در
`src/assets/authors/`. اگر نگذارید، نشان تاکسلی جایش می‌نشیند و مشکلی
پیش نمی‌آید.

شناسه‌های شبکه‌های اجتماعی همه اختیاری‌اند و هرکدام را بنویسید در
`sameAs` دادهٔ ساختاریافته هم می‌آید.

## ساخت پوستر و تصویر

چند اسکریپت هست که تصویرها را از روی هویت بصری می‌سازند، تا لازم نباشد
کسی با ابزار گرافیکی کار کند:

| دستور | خروجی |
|---|---|
| `npm run brand:anniversary` | پوستر سالگرد ۱۲۰۰×۱۲۰۰ |
| `npm run brand:mascots` | نُه ماسکات، هرکدام در دو رنگ |
| `npm run brand:social-kit` | آواتار و بنر شبکه‌های اجتماعی |
| `npm run brand:banner` | بنر ریدمی |
| `npm run brand:social` | پیش‌نمایش اجتماعی گیت‌هاب |

**پوستر سالگرد** پارامتر می‌گیرد:

```bash
node scripts/build-anniversary.mjs \
  --name=دبیان --latin=Debian --since=1993 --out=debian-33
```

`--until` را فقط برای چیزی بدهید که تمام شده. نبودش یعنی هنوز فعال است و
پوستر «تا امروز» می‌نویسد. وقتی `--until` بدهید، عدد بزرگ **طول دوره**
می‌شود نه سن امروز.

**ماسکات‌ها** با ویرایش `scripts/build-mascots.py` اضافه می‌شوند، نه با
کشیدن دستی، همین مورد است که شخصیت را یکدست نگه می‌دارد. هر حالت از همان
هندسهٔ نشان ساخته می‌شود.

## افزودن سالگرد

اگر سالگرد **به یک موجودیت وصل است**، به صورت خودکار تاریخ تولد اون توزیع یا پروژه را دریافت می‌کند و الویت حتما این است که
`firstRelease` را در فایل خودش بنویسید. صفحهٔ
[تقویم سالگردها](https://tuxly.ir/anniversaries) خودش می‌خواندش.

و اگر به پروژهٔ خاصی وصل نیست، اعلام گنو، نخستین GPL، زادروز یک نفر، در
`content/milestones.json` اضافه کنید:

```json
{
  "id": "gpl-v1",
  "date": "1989-02-25",
  "titleFa": "انتشار نخستین نسخهٔ GPL",
  "kind": "milestone",
  "entity": "projects/linux-kernel",
  "note": "یک تا دو جمله دربارهٔ اینکه چرا مهم بود.",
  "source": "https://example.org/original"
}
```

`kind` یکی از `founded`، `release`، `milestone` یا `person` است.
`entity`، `note` و `source` اختیاری‌اند، ولی اگر منبع دست‌اولی هست،
بگذاریدش.

**تاریخ را حتما میلادی بنویسید.** صفحه خودش شمسی نمایش می‌دهد و چون سال کبیسهٔ
دو تقویم یکی نیست، تاریخ شمسی هر سال یک روز جابه‌جا می‌شود، پس باید از
تاریخ میلادی محاسبه شود.

## پروانه

با ارسال PR می‌پذیرید که کدتان تحت [`AGPL-3.0-or-later`](https://www.gnu.org/licenses/agpl-3.0.html) و نوشته‌تان تحت
[`CC BY-SA 4.0`](https://creativecommons.org/licenses/by-sa/4.0/deed.fa) منتشر شود.

همچنین در بعضی موارد مثل استفاده از متن‌ها برای پروژه [آوای مشترک](https://commonvoice.mozilla.org/fa) نوشته به پروانه [`CC0 1.0`](https://creativecommons.org/publicdomain/zero/1.0/deed.fa) تغییر خواهد کرد.

اگه سوالی دارید می‌تونید [issue](https://github.com/Goudarz/tuxly/issues) باز کنید یا ایمیل بهم بدید: [hi@tuxly.ir](mailto:hi@tuxly.ir)
