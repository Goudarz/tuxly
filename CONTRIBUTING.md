# راهنمای مشارکت

خوش آمدید. هر اندازه کمک ارزشمند است — از اصلاح یک غلط املایی تا نوشتن یک
راهنمای یا اخبار.

## سریع‌ترین راه

پایین هر صفحهٔ سایت دکمهٔ **«ویرایش این صفحه در گیت‌هاب»** هست. اشتباهی دیدید،
همان‌جا اصلاحش کنید.

## راه‌اندازی محلی
پیشنهاد می‌کنم که ابتدا پروژه را [fork](https://github.com/goudarz/tuxly/fork) کنید بعد این مراحل را بروید.

برای هر تغییر در یک [Branch](https://git-scm.com/book/fa/v2/%d8%a7%d9%86%d8%b4%d8%b9%d8%a7%d8%a8%e2%80%8c%da%af%db%8c%d8%b1%db%8c-%d8%af%d8%b1-%da%af%db%8c%d8%aa-Git-Branching-%d8%b4%d8%a7%d8%ae%d9%87%e2%80%8c%d8%a8%d9%86%d8%af%db%8c-%d9%88-%d8%a7%d8%af%d8%ba%d8%a7%d9%85-%d9%be%d8%a7%db%8c%d9%87%e2%80%8c%d8%a7%db%8c-Basic-Branching-and-Merging) مجزا انجام بدید.

و همچنین به جای کلمه `USERNAME` نام‌کاربری گیت‌هاب خودتان را وارد کنید
```bash
git clone https://github.com/USERNAME/tuxly.git
cd tuxly
nvm use            # Node 24
npm install
npm run build      # الزامی است، ایندکس جست‌وجو را می‌سازد
npm run dev
```

اگر `npm run build` را رد کنید، همه‌چیز کار می‌کند جز جست‌وجو — چون ایندکس
Pagefind هنوز وجود ندارد.

## چه چیزی کمک می‌کند

- **اصلاح** غلط املایی، لینک شکسته، اطلاعات کهنه
- **مطلب تازه** — خبر، معرفی، آموزش، بررسی
- **معرفی موجودیت** — توزیع، میزکار، پنجره‌گردان، پروژه، اجتماع
- **رویداد** — هر نشست، کارگاه یا کنفرانس متن‌باز، داخلی یا خارجی
- **واژه‌نامه** — معادل فارسی اصطلاح فنی در `content/glossary/terms.json`

## نوشتن

از `content/posts/_template.md` کپی بگیرید. چند نکته:

**زمینهٔ فارسی اجباری است.** اگر مطلب از منبع بیرونی می‌آید، فیلد `context`
باید بگوید این موضوع برای خوانندهٔ فارسی‌زبان چه معنایی دارد. بدون آن بیلد
می‌شکند — و درست هم همین است، چون بدون زمینه مطلب ارزش افزوده‌ای ندارد.

**پیش‌نویس‌های خودکار مستقیم منتشر نمی‌شوند.** خط لولهٔ `pipeline/` پیش‌نویس
می‌سازد با `draft: true` و `originStatus: draft-review`. تا فردی بازبینی
نکند و `reviewedBy` را پر نکند، منتشر نمی‌شود.

**نسخه‌های توزیع‌ها را دستی ننویسید.** فیلدهای `currentVersion`، `releasedAt` و `eolAt`
را اسکریپت خودکار پر می‌کند. اگر دستی بنویسید، اجرای بعدی رونویسی‌شان می‌کند.

## نگارش فارسی

- نیم‌فاصله سر جایش: «می‌شود» نه «می شود»، «نرم‌افزار» نه «نرم افزار»
- «ی» و «ک» فارسی، نه عربی
- نام لاتین نرم‌افزارها را ترجمه نکنید: «GNOME»، نه «گنوم» در متن فنی —
  ولی در عنوان و متن روان فارسی‌نویسی اشکالی ندارد
- برای اصطلاح فنی، اول `content/glossary/terms.json` را ببینید تا معادل‌ها
  در کل سایت یکدست بماند
- عدد را فارسی ننویسید؛ قالب‌بندی خودکار انجام می‌شود

## پیام کامیت

قالب Conventional Commits، **به انگلیسی**. جزئیات و نمونه‌ها در
[`docs/COMMITS.md`](docs/COMMITS.md).

```
content(distro): add Void Linux entry
fix(rtl): keep version numbers in order inside Persian sentences
```

## قبل از ارسال PR

```bash
npm run check     # باید صفر خطا بدهد
npm run build     # باید بدون خطا تمام شود
```

## تصویر

- در `src/assets/` (در کنار مطالب هم امکانش هست قرار بگیرید ولی این کار را نکنید و در این مسیر قرار بدید)بگذارید
- **همیشه** `alt` بنویسید — بدون آن بیلد می‌شکند
- تبدیل به AVIF و WebP خودکار است؛ فایل اصلی را بهینه نکنید
- آواتار نویسنده: مربعی ۵۱۲×۵۱۲، `webp` یا `png`، زیر ۲۰۰ کیلوبایت

## پروانه

با ارسال PR می‌پذیرید که کدتان تحت [`AGPL-3.0-or-later`](https://www.gnu.org/licenses/agpl-3.0.html) و نوشته‌تان تحت
[`CC BY-SA 4.0`](https://creativecommons.org/licenses/by-sa/4.0/deed.fa) منتشر شود.

همچنین در بعضی موارد مثل استفاده از متن‌ها برای پروژه [آوای مشترک](https://commonvoice.mozilla.org/fa) نوشته به پروانه [`CC0 1.0`](https://creativecommons.org/publicdomain/zero/1.0/deed.fa) تغییر خواهد کرد.

اگه سوالی دارید می‌تونید [issue](https://github.com/Goudarz/tuxly/issues) باز کنید یا ایمیل بهم بدید: [hi@tuxly.ir](mailto:hi@tuxly.ir)
